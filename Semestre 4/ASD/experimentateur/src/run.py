import experience
import test 
import sorting

"""

exp = experience.Experience(5,10)



markers = exp.get_markers()
positive = exp.get_positive_markers()

negative = test.negative_markers3(markers, positive)

print(sorting.merge_sort(markers,test.compare))
print(sorting.merge_sort(positive,test.compare))
print(sorting.merge_sort(negative, test.compare))
print(cpt)

"""

test.run2(100)